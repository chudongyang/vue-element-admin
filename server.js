const express = require('express');
const jwt = require('jsonwebtoken'); // 用来生成token的
const bodyParser = require('body-parser'); //
const app = express();
app.use('/api', function name(params) {

})
app.post('/login')

app.listen(3000, function() {
  console.log(`3000 port is running!`);
})
